// 03menu.cpp : Defines the entry point for the console application.
//
//Rafael Camacho 15.08.17

//librerias

#include "stdafx.h"
#include <iostream>
#include "conio.h"
#include "stdlib.h"



using namespace std;

//Prototipo de funciones
float POTEN(float x,int y);
int FAC(int n);
float serie(int X,int N);

//cuerpo principal
int main ()

{ int opcion,serie,exp;
  float pot,fac,base,n;

  do
  {cout<<"Menu \n";
   cout<<"1.-potencia \n";
   cout<<"2.-factorial \n";
   cout<<"3.-serie \n";
   cout<<"0.-Salir \n";
   cout<<"Opcion: ";
   cin>>opcion;
   switch(opcion)
   {
		case 1:cout<<"Ingrese la base: ";
			   cin>>base;
			   cout<<"Ingrese el exponente: ";
			   cin>>exp;
			   pot=POTEN(base,exp);
			   cout<<"La potencia de "<< base <<" elevado a: "<<exp<<" es: "<<pot;
			   break;
		case 2:cout<<"Ingrese el valor n";
			   cin>>n;
			   fac=FAC(n);
			   cout<<"El factorial de "<< n <<" es igual a: "<<fac;
			   break;
		case 3:cout<<"Ingrese el valor de X: ";
			   cin>>;
			   cout<<"Ingrese el valor de Y : ";
			   cin>>;
			   S= serie(base,exponente); //Lamacda a POten
			   break;
		case 0:cout<<"salir\n";
			   break;
		default:cout<<"Opcion invalida";
  }
   getch();
   system("cls");
  }while(opcion!=0);

   return 0;
}

float POTEN(float x,int y)
{ float P=1;   //Acumulador de producto
  for (int i=1;i<=y;i++)
       P=P*x;
  return P;
}


int FAC(int n)
{ int P=1,i;   //Acumulador de producto
  for (int i=2;i<=n;i++)
       P=P*i;
  return P;
}
float serie(int X,int N)
{ 
}
